load('rt/util.js');

function foo(n) {
    return (function(x) {
        return (function(n, c) {
            if ((function(x) {
                return (patListTest)([
                    function(x) {
                        return true;
                    },
                    function(x) {
                        return true;
                    }
                ], x);
            })(n)) return (((1 + (x[0] * x[0])) + x[1]) + FUNCALL_TO_BE_DONE());
            else return (c)(n);
        })(x, fail);
    })(n);
}
